# Razorpay Flutter (Android only)

This is the Android portion of `razorpay_flutter` 1.4.5. Talkieo uses Apple
In-App Purchase on iOS, so this package deliberately has no iOS plugin
registration and contributes no Razorpay framework to the App Store binary.
